services:
  mvd-aggregator:
    build: .
    image: mvd-aggregator:latest
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:3001:3001"
    networks:
      - web

networks:
  web:
    external: true
